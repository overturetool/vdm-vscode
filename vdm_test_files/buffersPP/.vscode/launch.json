{
    "configurations": [
        {
            "name": "Launch VDM Debug from getBuffers()`Buffers",
            "type": "vdm",
            "request": "launch",
            "noDebug": false,
            "defaultName": "getBuffers()",
            "command": "print new Buffers().getBuffers()"
        }
    ]
}
